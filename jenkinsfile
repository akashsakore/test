pipeline {
    agent {
        label 'jenkins-agent-1'
    }
    tools {
        maven 'maven'
        jdk 'java17'
    }
    stages {
        stage("checkout from scm") {
            steps {
                git branch: 'main', credentialsId: 'github', url: 'https://github.com/akashsakore/test.git' 
            }
        }
        stage("build app") {
            steps {
                sh 'mvn clean packege'
            }
        }
        stage("test app") {
            steps {
                sh 'mvn test'
            }
        }
    }
}
