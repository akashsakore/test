pipeline {
    agent {
        label 'jenkins-node-1'
    }
    tools {
        jdk 'java17'
        maven 'maven'
    }
    stages {
        stage('checkout') {
            steps {
                echo 'Hello World'
            }
        }
    }
}
