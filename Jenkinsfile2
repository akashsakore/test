pipeline {
    agent {
        label 'jenkins-node-1'
    }
    triggers {
        pollSCM('')
    }
    tools {
        jdk 'java17'
        maven 'maven'
    }
    stages {
        stage('checkout') {
            steps {
                git branch: 'main', credentialsId: 'github', url: 'https://github.com/akashsakore/test.git'
            }
        }
    }
}
